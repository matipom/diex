 	let c;
    let a = 34;
    let b = 21;

    console.log(a+b) //first expression
    // Prediction: it will output 55 because a and b are numbers
    // Actual: 55


      a = 2;

    console.log(a+b) //second expression
    // Prediction: it will output 23 because the value of a change to 2
    // Actual: 23



    //the value of c is a number



    console.log(3 + 4 + '5');
    //prediction: 75 . 7 because '3+4 are numbers' and then because 5 is a string just will concatenate 7+5
    //actual: 75